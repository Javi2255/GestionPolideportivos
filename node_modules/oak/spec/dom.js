"use strict";

describe("dom", function () {

    it("should exist", function () {
        expect(oak.build).toBeDefined();
    });

    it("should exist", function () {
        expect(oak.contains).toBeDefined();
    });

    it("should exist", function () {
        expect(oak.find).toBeDefined();
    });

    it("should exist", function () {
        expect(oak.findAll).toBeDefined();
    });

    it("should exist", function () {
        expect(oak.hasClass).toBeDefined();
    });

    describe("dom strap", function () {
        it("should strap", function () {
            var div = document.createElement("DIV"),
                strap = oak.strap(div);

            expect(strap.css).toBeDefined();
        });
    });

});
